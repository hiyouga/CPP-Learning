#include <iostream>
#define NOTLE ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

using namespace std;

typedef unsigned long long ull;

int main()
{
    NOTLE
    int t;
    ull a, b, c, d, p;
    cin >> t;
    while(t--){
        cin >> a >> b >> c >> d >> p;
    }
}
