#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
	short n;
	while(cin >> n){
		int a[n];
		short cnt[n][n] = {0};
		for(short i = 0; i < n; i++)
			cin >> a[i];
		sort(a, a+n);
		for(short i = 0; i < n; i++){
			a[n]
		}
	}
	return 0;
}