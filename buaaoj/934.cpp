#include <iostream>
#include <algorithm>
#define NOTLE ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define MOD 1e7+7
using namespace std;

int main()
{
	NOTLE

	int n;
	cin >> n;
	n -= 5;
	long long a = 1, b = 1;
	while (n) {
		
	}
}